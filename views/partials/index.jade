p There are {{items.length}} items
div.container
	div.table-responsive
		table.table
			tr
				th
				th
				th(ng-click="sort('Name')")
					label(for='Name') Name
			tr(ng-repeat="item in items | orderBy:sortCol:sortDesc")
				td
					a(href='editItem/{{item.id}}') Edit
					|  - 
					a(href='deleteItem/{{item.id}}') Delete						
				td
					{{item.Name}}
